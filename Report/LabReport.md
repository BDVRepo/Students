# ЛАБОРАТОРНАЯ РАБОТА

## Создание ленточных форм для работы с данными

**Дисциплина:** Разработка приложений Windows Forms

**Вид работы:** Лабораторная работа

**Исполнитель:** Bashkirov

**Руководитель:** [Указать ФИО руководителя]

**Город, год:** [Указать город и год]

---

## Введение

**Цель работы:** Научиться создавать сложные ленточные формы для работы с данными.

**Задание:** Выполнить разработку формы, добавление команд для работы с записями, настройку маски ввода, использование выпадающих списков.

**Исходные данные:**
- База данных `students` с таблицами: Предметы, Специальности, Студенты, Оценки
- Microsoft Visual Studio
- MS SQL Server
- SQL Server Management Studio

---

## 1. Теоретическая часть

### 1.1. Windows Forms и Data Binding

Windows Forms — это технология создания пользовательских интерфейсов для приложений Windows на платформе .NET Framework. Data Binding (привязка данных) позволяет связывать элементы управления с источниками данных, обеспечивая автоматическое отображение и обновление информации.

### 1.2. MaskedTextBox и маски ввода

MaskedTextBox — это элемент управления, который ограничивает ввод данных в соответствии с заданной маской. Маски ввода позволяют:
- Обеспечить правильный формат данных (телефон, паспорт, номер зачетки)
- Упростить ввод для пользователя
- Предотвратить ошибки ввода

Примеры масок:
- `00000` — числовая маска (5 цифр)
- `0000-000000` — паспортные данные
- `+7(000)000-00-00` — телефонный номер

### 1.3. ComboBox и привязка данных

ComboBox (выпадающий список) позволяет пользователю выбирать значение из предопределенного списка. При использовании привязки данных ComboBox может:
- Отображать значения из таблицы базы данных
- Автоматически обновляться при изменении данных
- Связывать отображаемое значение с другим полем (DisplayMember и ValueMember)

### 1.4. BindingSource и навигация

BindingSource — это компонент, который обеспечивает:
- Привязку данных между элементами управления и источниками данных
- Навигацию по записям (MoveFirst, MovePrevious, MoveNext, MoveLast)
- Добавление, удаление и редактирование записей
- Валидацию данных

---

## 2. Результаты работы

### 2.1. Главная форма (Form1)

Главная форма приложения содержит:
- Два Label: "База данных" и "Bashkirov" (шрифт 32pt, Times New Roman)
- Четыре кнопки: "Таблица 1", "Таблица 2", "Таблица 3", "Таблица 4"
- Кнопка "Таблица 1" открывает форму для работы с таблицей "Студенты"

**Рис. 9.1 – Главная кнопочная форма в режиме конструктора**

*[Здесь должна быть вставлена экранная копия главной формы]*

### 2.2. Форма студентов (Form2)

Форма для работы с таблицей "Студенты" включает:

#### 2.2.1. Панель навигации (BindingNavigator)

В верхней части формы расположена стандартная панель навигации с кнопками:
- `|<` — переход к первой записи
- `<` — переход к предыдущей записи
- `>` — переход к следующей записи
- `>|` — переход к последней записи
- `+` — добавление новой записи
- `X` — удаление текущей записи
- 💾 — сохранение изменений

**Рис. 20.25 – Форма для таблицы «Студенты»**

*[Здесь должна быть вставлена экранная копия формы студентов]*

#### 2.2.2. Заголовок формы

Заголовок "Таблица Bashkirov Студенты" отображается курсивом, шрифт 24pt.

#### 2.2.3. Поля формы

Форма содержит следующие поля:

1. **Код студента** — TextBox (только чтение, автоинкремент)
2. **ФИО** — TextBox
3. **Пол** — ComboBox с элементами "Мужской", "Женский"
4. **Дата рождения** — DateTimePicker
5. **Родители** — ComboBox с элементами "Отец и Мать", "Мать", "Отец", "Нет"
6. **Адрес** — TextBox
7. **Телефон** — MaskedTextBox с маской `+7(000)000-00-00`
8. **Паспортные данные** — MaskedTextBox с маской `0000-000000`
9. **Номер зачётки** — MaskedTextBox с маской `00000` (Numeric 5-digits)
10. **Дата поступления** — DateTimePicker
11. **Группа** — TextBox
12. **Курс** — NumericUpDown (минимум 1, максимум 5)
13. **Код специальности** — ComboBox, связанный с таблицей Специальности
14. **Очная форма обучения** — CheckBox

**Рис. 9.9 – Вид формы с полями**

*[Здесь должна быть вставлена экранная копия формы с полями]*

**Рис. 20.10 – Форма после удаления полей**

*[Здесь должна быть вставлена экранная копия формы после удаления текстовых полей]*

#### 2.2.4. Настройка масок ввода

Маски ввода настраиваются через контекстное меню элемента MaskedTextBox, пункт "Set Mask...".

**Рис. 20.12 – Меню действий для задания маски**

*[Здесь должна быть вставлена экранная копия меню действий]*

**Рис. 20.13 – Окно задания маски**

*[Здесь должна быть вставлена экранная копия окна задания маски]*

**Рис. 20.14 – Маска для паспортных данных**

*[Здесь должна быть вставлена экранная копия настройки маски для паспортных данных]*

**Рис. 20.15 – Маска для телефона**

*[Здесь должна быть вставлена экранная копия настройки маски для телефона]*

#### 2.2.5. Настройка выпадающих списков

Для поля "Пол" элементы списка задаются через String Collection Editor:

**Рис. 20.18 – Окно для редактирования списка полов**

*[Здесь должна быть вставлена экранная копия редактора коллекции строк]*

Для поля "Код специальности" используется привязка данных:

**Рис. 20.20 – Меню действий для выпадающего списка**

*[Здесь должна быть вставлена экранная копия меню действий ComboBox]*

**Рис. 20.22 – Окончательный вид панели действий**

*[Здесь должна быть вставлена экранная копия панели действий с настройками привязки данных]*

Настройки:
- Data Source: `StudentsDataSet.Специальности`
- Display Member: `Наименование специальности`
- Value Member: `Код специальности`
- Selected Value: `студентыBindingSource.Код специальности`

#### 2.2.6. Кнопки навигации

Справа от полей формы расположены семь кнопок навигации:

**Рис. 20.2 – Форма с кнопками**

*[Здесь должна быть вставлена экранная копия формы с кнопками]*

1. **"Первая"** — переход к первой записи
2. **"Предыдущая"** — переход к предыдущей записи
3. **"Добавить"** — добавление новой записи
4. **"Последняя"** — переход к последней записи
5. **"Следующая"** — переход к следующей записи
6. **"Удалить"** — удаление текущей записи
7. **"Сохранить"** — сохранение изменений в базу данных

---

## 3. Листинги программного кода

### 3.1. Form1.cs — обработчик кнопки "Таблица 1"

```csharp
private void button1_Click(object sender, EventArgs e)
{
    Form2 newForm = new Form2();
    newForm.Show();
}
```

### 3.2. Form2.cs — обработчики кнопок навигации

```csharp
private void button1_Click(object sender, EventArgs e)
{
    // Первая запись
    студентыBindingSource.MoveFirst();
}

private void button2_Click(object sender, EventArgs e)
{
    // Предыдущая запись
    студентыBindingSource.MovePrevious();
}

private void button3_Click(object sender, EventArgs e)
{
    // Добавить новую запись
    студентыBindingSource.AddNew();
}

private void button4_Click(object sender, EventArgs e)
{
    // Последняя запись
    студентыBindingSource.MoveLast();
}

private void button5_Click(object sender, EventArgs e)
{
    // Следующая запись
    студентыBindingSource.MoveNext();
}

private void button6_Click(object sender, EventArgs e)
{
    // Удалить текущую запись
    студентыBindingSource.RemoveCurrent();
}

private void button7_Click(object sender, EventArgs e)
{
    // Сохранить изменения
    this.Validate();
    this.студентыBindingSource.EndEdit();
    this.tableAdapterManager.UpdateAll(this.studentsDataSet);
}
```

### 3.3. App.config — строка подключения к базе данных

```xml
<connectionStrings>
    <add name="StudentsConnectionString" 
         connectionString="Data Source=localhost;Initial Catalog=students;User ID=sa;Password=MSSQL" 
         providerName="System.Data.SqlClient" />
</connectionStrings>
```

---

## 4. Ответы на контрольные вопросы

### 4.1. Для выполнения каких действий предназначены кнопки на основной форме?

Кнопки на основной форме (Form1) предназначены для открытия различных таблиц базы данных. В данном проекте реализована кнопка "Таблица 1", которая открывает форму для работы с таблицей "Студенты". Кнопки "Таблица 2", "Таблица 3", "Таблица 4" могут быть использованы для открытия других таблиц базы данных (Предметы, Специальности, Оценки).

### 4.2. Какие бывают маски ввода?

В проекте использованы следующие маски ввода:

1. **Numeric (5-digits)** — маска `00000` для номера зачетки. Обеспечивает ввод ровно 5 цифр.

2. **Custom маска для паспортных данных** — маска `0000-000000`. Обеспечивает ввод 4 цифр, тире, затем 6 цифр.

3. **Custom маска для телефона** — маска `+7(000)000-00-00`. Обеспечивает ввод телефонного номера в формате: код страны, код города в скобках, номер телефона с дефисами.

Маски ввода могут быть:
- Предопределенными (Numeric, Phone number, Date, Time и др.)
- Пользовательскими (Custom), задаваемыми вручную с использованием символов:
  - `0` — обязательная цифра
  - `9` — необязательная цифра
  - `#` — цифра или пробел
  - `L` — обязательная буква
  - `?` — необязательная буква
  - `&` — обязательный символ
  - `C` — необязательный символ
  - `A` — буква или цифра
  - `.` — десятичный разделитель
  - `,` — разделитель тысяч
  - `:` — разделитель времени
  - `/` — разделитель даты

### 4.3. Как задаётся источник данных?

Источник данных задается через окно "Источники данных" (Data Sources) в Visual Studio:

1. **Создание подключения к базе данных:**
   - Меню "Данные" → "Добавить новый источник данных" (или "Вид" → "Другие окна" → "Источники данных")
   - Выбор "База данных" → "Набор данных"
   - Создание подключения к SQL Server
   - Выбор базы данных `students`

2. **Создание DataSet:**
   - Выбор таблиц для включения в DataSet (Студенты, Специальности)
   - Visual Studio автоматически создает DataSet с таблицами и их структурами

3. **Создание TableAdapter:**
   - Для каждой таблицы автоматически создается TableAdapter с методами Fill и Update

4. **Создание BindingSource:**
   - BindingSource создается автоматически при перетаскивании таблицы на форму
   - BindingSource связывает DataSet с элементами управления

5. **Привязка элементов управления:**
   - Поля таблицы перетаскиваются из окна "Источники данных" на форму
   - Visual Studio автоматически создает элементы управления и привязывает их к BindingSource

6. **Настройка привязки данных для ComboBox:**
   - Для ComboBox с привязкой к другой таблице настраиваются свойства:
     - Data Source: источник данных (например, `StudentsDataSet.Специальности`)
     - Display Member: поле для отображения (например, `Наименование специальности`)
     - Value Member: поле для значения (например, `Код специальности`)
     - Selected Value: привязка к полю текущей записи (например, `студентыBindingSource.Код специальности`)

---

## Заключение

В ходе выполнения лабораторной работы была создана Windows Forms приложение для работы с базой данных студентов. Реализованы следующие задачи:

1. **Создана главная форма** с кнопками для открытия различных таблиц базы данных.

2. **Разработана форма для работы с таблицей "Студенты"**, включающая:
   - Панель навигации (BindingNavigator) для перемещения по записям
   - Все необходимые поля для отображения и редактирования данных студентов
   - Маски ввода для телефона, паспортных данных и номера зачетки
   - Выпадающие списки (ComboBox) для пола, родителей и специальности
   - Числовой счетчик (NumericUpDown) для курса
   - Кнопки навигации для программного управления записями

3. **Настроена привязка данных** между элементами управления и базой данных через DataSet, TableAdapter и BindingSource.

4. **Реализованы операции** добавления, редактирования, удаления и сохранения записей.

Все поставленные задачи выполнены полностью. Приложение готово к использованию и позволяет эффективно работать с данными о студентах через удобный пользовательский интерфейс.

---

## Приложение

### SQL скрипт создания базы данных

Скрипт создания базы данных `students` находится в файле `Database/create_database.sql`.

*[Здесь может быть вставлен полный текст SQL скрипта или ссылка на файл]*

